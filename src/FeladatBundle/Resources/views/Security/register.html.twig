{% extends '::base.html.twig' %}

{% block title %}Regisztráció{% endblock %}
{% form_theme form 'FeladatBundle:Form:fields.html.twig'%}
{% block menu %}{% endblock %}
{% block body %}
        {#{% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}#}

        <div class="register-container">
            <div class="register-content">
                {{ form_start(form) }} 
                {{ form_row(form.nev_elotag) }}
                {{ form_row(form.vezeteknev) }}<br>
                {{ form_row(form.keresztnev) }}<br>
                {{ form_row(form.username) }}<br>
                {{ form_row(form.password) }}<br>
                {{ form_row(form.submit) }}
                {{ form_end(form) }}
            </div>
        </div>
{% endblock %}
